<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ML_demo"
        tools:targetApi="31"
        tools:replace="android:allowBackup, android:label">

        <activity android:name=".common.EmptyActivity"
            android:exported="false"
            android:label="空Activity" />

        <activity android:name=".common.BaseActivity"
            android:exported="false"
            android:label="BaseActivity" />

        <activity android:name=".main.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".mobilesam.MainActivity"
            android:exported="false"
            android:label="mobilesam入口" />

        <activity android:name=".u2net.MainActivity"
            android:exported="false"
            android:label="u2net入口" />

        <activity android:name=".lama.MainActivity"
            android:exported="false"
            android:label="lama入口" />

        <activity android:name=".patchmatch.MainActivity"
            android:exported="false"
            android:label="patchmatch入口" />

        <activity android:name=".opencv.MainActivity"
            android:exported="false"
            android:label="opencv入口" />

        <activity android:name=".u2net.DisplaySegmentActivity"
            android:exported="false"
            android:label="显著性裁剪结果" />

        <activity android:name=".u2net.Display3dActivity"
            android:exported="false"
            android:label="3D效果" />

    </application>

</manifest>